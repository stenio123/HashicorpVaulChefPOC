# Automatically generated by Chef/ChefNodes/Vagrantfile.erb
# Vagrantfile creating dynamic Chef Nodes
#
# NOTE: Make sure you have the centos/7 base box installed...
# vagrant box add centos/7

Vagrant.configure("2") do |config|
<% @node.each do |name, box, ip, hostname, environment| %>
  config.vm.define <%=name%> do |chef|
    chef.vm.box = <%=box%>
    chef.vm.hostname = <%=hostname%>
    chef.vm.network :hostonly  <%=ip%>
    chef.vm.provider :virtualbox do |vb|
      vb.memory = 256
    end
    node1.vm.provision "shell" do |s|
      s.path = "install-node.sh"
    end
  end
<% end %>
end
